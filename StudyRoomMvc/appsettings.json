{
  // ** To avoid having to re-enter connection string every time we pull changes
  // add the connection string to user secrets instead and change it to your DB. **
  // - Miguel
  //"ConnectionStrings": {
  //  "AzureSqlDb": "Server=tcp:csdevdb-heritagecs.database.windows.net,1433;Database=H60_StudyRoomDb_ES;Authentication=Active Directory Interactive;Encrypt=True;TrustServerCertificate=False;MultipleActiveResultSets=True;"
  //},
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
